import 'package:flutter/material.dart';

class TodoCellItem extends StatelessWidget {
  const TodoCellItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
